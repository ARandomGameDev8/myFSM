# myFSM installers (one per engine runtime)

Each engine runtime ships its own installer with the same traditional UX:
detect the engine, fetch the tool, discover the user's projects, pick one,
pick an in-project location, install there.

| Folder | Engine | Status |
|---|---|---|
| `unity/` | Unity (Python, stdlib only) | implemented |
